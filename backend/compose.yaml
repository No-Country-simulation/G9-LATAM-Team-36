services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: energiai
      POSTGRES_USER: energiai
      POSTGRES_PASSWORD: change_me
    ports:
      - "5432:5432"
    volumes:
      - energiai_pgdata:/var/lib/postgresql/data

volumes:
  energiai_pgdata:
